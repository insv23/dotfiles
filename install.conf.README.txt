# 注意事项

link 的格式是 `目标位置: 当前目录下的位置`
新建条目时不要手动打, 映射之间的"空白"是 tab 还是 space 要求很严格, 从已有项目复制

先在当前目录有, 然后执行 ./install 命令将当前目录的文件或目录链接到所需位置
- 如果外面已经有了文件或目录, 需要我们手动删除或挪走
- 有些软件比较顽固, 删除后马上又自己生成了, 这时就要用 `rm -rf ~/.config/atuin/ && ./instal` 这种组合拳

如果是一个目录, 必须在后面加上 /, 否则无法成功追踪

submodule 我还是不理解怎么用