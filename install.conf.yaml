- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.bashrc:
    ~/.gitconfig:
    ~/.hushlogin:
    ~/.profile:
    ~/.p10k.zsh:
    ~/.tmux:
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.zsh:
    ~/.zshrc:
            
- shell:
  - git submodule sync --recursive
  # the following have to run after linking ~/.gitconfig
  - git update-submodules
