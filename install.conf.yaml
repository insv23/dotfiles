- defaults:
    link:
      create: true
      relink: true

- clean: ["~"]

- link:
    ~/.bashrc:
    ~/.config/atuin/: ./atuin
    ~/.config/karabiner/: ./karabiner
    ~/.config/kitty/: ./kitty
    ~/.config/nvim/: ./nvim
    ~/.config/yazi/: ./yazi
    ~/.gitconfig:
    ~/.hammerspoon:
    ~/.profile:
    ~/.p10k.zsh:
    ~/.tmux:
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.zsh:
    ~/.zshenv: ./zsh/zshenv
    ~/.zshrc:

- shell:
    - touch ~/.hushlogin
    - ./zsh/install/0.pre.sh > /dev/tty
    - ./zsh/install/1.brewInstallApps.sh > /dev/tty
    - git submodule sync --recursive
    - git submodule update --init --recursive
    - >
      git clean -fdx
      vim/pack/vendor/start/
      zsh/plugins/
      tmux/plugins/
  # the following have to run after linking ~/.gitconfig
